#include <stdio.h>
#include <math.h>

int main()
{
    int epoch = 6;
    double x;
    scanf("%lf", &x);

    double y = 0, dx = x;
    for (int i = 1; 1 <= epoch * 2; i += 2)
    {
        y += (dx / i - (dx *= x) / (i += 2));
    }

    print("")
}